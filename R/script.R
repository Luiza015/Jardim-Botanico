#script para ler a tabela limpa
#os dados originais estao em um xlsx

help(read.table)
read.csv2("./data/ex04(formatado).csv")
#read.csv2 para linguas latinas (separador ;); sep + ";", dec = "," para substituir o separador por ponto
dir("data")
